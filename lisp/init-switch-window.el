;;; init-switch-window --- Summary
;;; Commentary:
;;; Code:

(use-package switch-window
  :ensure t
  :config
  (global-set-key (kbd "C-x o") 'switch-window))

(provide 'init-switch-window)
;;; init-avy ends here
